describe('returnLanguages', () => {
  it('calls a ruby script when the language is ruby', () => {
    const input = input
    const output = output
    const language = 'ruby'
    expect(findMethod([1, 2, 3], '1,2,3')).toContain('.join');
  });
});
